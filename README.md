# hummingbird

```
docker-compose up --build
docker-compose run --rm hummingbird bundle install
docker-compose run --rm hummingbird rails db:create
docker-compose run --rm hummingbird rails db:migrate
docker-compose up
```
